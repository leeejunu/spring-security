<!-- mapper DTD 선언 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.security.mapper.UserRoleMapper">
    <insert id="insert">
        insert into user_role_tb
        values (0, #{user_id}, #{roleId}, now(), null);
    </insert>
</mapper>